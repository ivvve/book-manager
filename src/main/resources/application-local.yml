# Spring configs
spring:
  # Liquibase
  liquibase:
    enabled: false

  # Vault
  cloud:
    vault:
      enabled: false

# Application specific configs
app:
  service:
    email-domain: devson.com
